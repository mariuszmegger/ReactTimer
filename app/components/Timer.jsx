var React = require('react');

var Timer = React.createClass({
    render : function(){
        return (
            <div>
                <p>Timer.jsx rendered</p>
            </div>
        );
    }
})

module.exports = Timer;
